
##  Repository Pattern

O padrão repository é como o nosso  codigo vai conectar com a camada de persistencia (banco de dados)
Os repositorios fazem as relacoes entre as camadas externas, como camada de persisitencia (banco de dados), com as camadas internas do nosso codigo (entities,use-cases)

